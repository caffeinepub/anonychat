// Feature flags — set to false to disable a feature without deleting code
export const FEATURES = {
  chat: true,
  randomChat: true,
  discover: true,
  p2pMarket: true,
  anonCash: true,
  voiceMessages: true,
  qrCode: true,
  premiumId: true,
  notifications: true,
} as const;
